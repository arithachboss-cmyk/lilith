# Claim register — PKG-RFID-READER

ดราฟต์นี้ **ไม่มีตัวเลข ไม่มีชื่อยี่ห้อ ไม่มีชื่อรุ่น และไม่มีราคา**

## claim ที่หน้านี้ใช้จริง

| claim | class | ใช้อย่างไร |
|---|---|---|
| `CLM-S-005` | SAFE_WORDING | ต้องทดสอบกับของจริง — ใช้ในส่วนที่อธิบายว่าเครื่องตัวเดียวกันให้ผลต่างกันเมื่อเปลี่ยนป้าย |
| `CLM-S-006` | SAFE_WORDING | สี่คำถามก่อนคุยกับผู้ขาย และตารางสามรูปแบบ เป็นกรอบเกณฑ์ ไม่ใช่การจัดอันดับ |

## claim ที่จงใจไม่ใช้

| claim | class | อยากพูดอะไร | ทำไมไม่พูด | ใครเป็นคนปิด |
|---|---|---|---|---|
| `CLM-E-001` | EVIDENCE_REQUIRED | ระยะที่แต่ละรูปแบบทำได้ | ต้องอ้างรุ่นและหน้า datasheet ที่แน่นอน — บทความอธิบายว่าเป็นผลร่วมของหลายตัวแปรแทน | ผู้ผลิต (`SRC-VEN-002`) |
| `CLM-E-002` | EVIDENCE_REQUIRED | อ่านครบกี่เปอร์เซ็นต์ของกอง | ตัวเลขอัตราการอ่านถูกสั่งลบแล้ว (`CLM-B-001`) — บทความพูดถึงการบังเชิงกลไกแทน | ผู้ผลิต (`SRC-VEN-002`) |
| `CLM-E-007` | EVIDENCE_REQUIRED | ระดับการป้องกันฝุ่นน้ำของเครื่องที่ติดกลางแจ้ง | เป็นตัวเลขที่ผู้ผลิตประกาศต่อรุ่น — บทความชี้ให้ไปอ่านเอกสารทางการพร้อมเงื่อนไขการทดสอบ | ผู้ผลิต (`SRC-VEN-002`) |
| `CLM-E-008` | EVIDENCE_REQUIRED | หนึ่งเครื่องรับเสาอากาศได้กี่ต้น | เป็นสเปกต่อรุ่น — บทความบอกว่าเป็นตัวแปรที่กระทบงบ โดยไม่ระบุจำนวน | ผู้ผลิต (`SRC-VEN-002`) |
| `CLM-E-011` | EVIDENCE_REQUIRED | เอ่ยชื่อผู้ผลิต | บนหน้าที่ว่าด้วยการเลือกเครื่อง การเอ่ยชื่อถูกอ่านเป็นการแนะนำเสมอ | ผู้ผลิต |
| `CLM-O-001` | OWNER_REQUIRED | เครื่องแบบไหนคุ้มกว่า | เป็นการเปรียบเทียบราคา ต้องมีราคาทั้งสองฝั่งพร้อมวันที่ — `redact.mjs` จับคำนี้ได้จริงในรอบแรก | ACS (`SRC-ACS-002`) |
| `CLM-O-002` | OWNER_REQUIRED | ACS ขายเครื่องอ่านรูปแบบใดบ้าง | ยังไม่มีรายการสินค้าที่ยืนยัน | ACS (`SRC-ACS-001`) |
| `CLM-B-004` | BLOCKED | ฟันธงว่ารูปแบบใดหรือยี่ห้อใดเหนือกว่า | ห้ามถาวร — ตารางสามแบบจึงมีประโยคกำกับว่าไม่มีแบบที่เหนือกว่า | ไม่มีใคร |

## ข้อสังเกตที่ควรอยู่ในสายตา Owner

หน้านี้เป็น `page_type: PRODUCT` แต่ดราฟต์ไม่ระบุสินค้าใดเลย เพราะ `SRC-ACS-001` ยังไม่มา

นี่ไม่ใช่ข้อบกพร่องของดราฟต์ แต่เป็นสิ่งที่ Owner ควรเห็น: **หน้าสินค้าที่ไม่มีรายการสินค้า
จะทำได้ดีที่สุดแค่เป็นหน้าให้ความรู้** และจะเป็นแบบนั้นต่อไปจนกว่า `SRC-ACS-001` จะถูกส่ง
