
LILITH_ADMIN_EMAIL=
LEAD_IMPORT_TOKEN=
# Optional matching weights. Only NEW matches use a changed policy (immutable snapshots).
# MATCH_WEIGHTS_JSON={"budget":40,"location":30,"bedrooms":20,"facilities":10}

# Local isolated testing only. No setting in this build enables real lead intake.
# MIDDLE_READINESS_MODE=mock
# MIDDLE_OPERATIONS_USER_IDS= (test fixture only; real authorization remains unconfirmed)
